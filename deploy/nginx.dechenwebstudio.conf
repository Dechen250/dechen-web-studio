# Blocos para colar no proxy da borda (80/443).
# Só dechenwebstudio.com.br. Não altere os server_name de cliente.

server {
    listen 80;
    server_name dechenwebstudio.com.br www.dechenwebstudio.com.br;

    client_max_body_size 8m;

    location /.well-known/acme-challenge/ {
        root /var/www/certbot;
        try_files $uri =404;
    }

    location / {
        proxy_pass http://172.17.0.1:3003;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
        proxy_read_timeout 180s;
        proxy_buffering off;
    }
}

server {
    listen 443 ssl;
    server_name dechenwebstudio.com.br www.dechenwebstudio.com.br;

    ssl_certificate     /etc/nginx/certs/dws.crt;
    ssl_certificate_key /etc/nginx/certs/dws.key;

    client_max_body_size 8m;

    location / {
        proxy_pass http://172.17.0.1:3003;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto https;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
        proxy_read_timeout 180s;
        proxy_buffering off;
    }
}
